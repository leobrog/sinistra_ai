We are going to create a discord bot using bun, as you can see by the project setup. This bot will have slash commands and will need permissions to be able to send messages in channels, read messages in channels, send dms, read dms, and check people's roles within the server. 

The bot is called Sinistra and it's going to be the Background Simulation assistant for Elite Dangerous, created to serve Communism Interstellar and it's allies. 

Given this short context, setup this discord bot, taking these points into consideration:

- Establish a concise framework for the bot that will allow me to easily create new commands and implement new features. 

- For now implement only one command that is going to use the Gemini API to chat with users in a channel or via dms.

- We'll load all environment variables through .env files.

- I want the application to be dockerized so that we can run them in a container in my server later.

